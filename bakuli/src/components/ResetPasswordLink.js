import React from "react";
import ResetPassword from "./ResetPassword";

const ResetPasswordLink = () => {
    return (
        <ResetPassword title="Password Reset Link">
        <p>We have emailed you a password reset link, check your email.</p>
        </ResetPassword>

    )
                
}

export default ResetPasswordLink;